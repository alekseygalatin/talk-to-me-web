import { useParams } from 'react-router-dom';

export function Chat() {
  const { partnerId } = useParams();
  
  // Use partnerId to customize the chat experience
  // ... rest of your existing chat component code

  return (
      {}
  );
} 